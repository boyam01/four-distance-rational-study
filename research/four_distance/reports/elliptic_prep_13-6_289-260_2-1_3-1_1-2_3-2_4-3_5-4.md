# Elliptic Prep

This file only prepares exact cleared-denominator curve equations.
It does not assert rank, rational point completeness, or non-existence.

## Sage Status

Sage not found; genus/rank attempts skipped.

## delta = 13/6

A = 2u/(1-u^2)

### C in S

C(u) = (3*u**2 + 13*u - 3) / (6*u)

Cleared denominator condition:

```text
W_C^2 = 9*u**4 + 78*u**3 + 187*u**2 - 78*u + 9
denominator_square = 36*u**2
```

### B in S

B(u) = (7*u) / (-3*(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = 9*u**4 + 31*u**2 + 9
denominator_square = 9*(u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (3*u**2 + 13*u - 3) / (7*u)

Cleared denominator condition:

```text
W_D^2 = 9*u**4 + 78*u**3 + 200*u**2 - 78*u + 9
denominator_square = 49*u**2
```

## delta = 289/260

A = 2u/(1-u^2)

### C in S

C(u) = (130*u**2 + 289*u - 130) / (260*u)

Cleared denominator condition:

```text
W_C^2 = 16900*u**4 + 75140*u**3 + 117321*u**2 - 75140*u + 16900
denominator_square = 67600*u**2
```

### B in S

B(u) = (29*u) / (-130*(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = 16900*u**4 - 32959*u**2 + 16900
denominator_square = 16900*(u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (130*u**2 + 289*u - 130) / (29*u)

Cleared denominator condition:

```text
W_D^2 = 2*(8450*u**4 + 37570*u**3 + 25281*u**2 - 37570*u + 8450)
denominator_square = 841*u**2
```

## delta = 2

A = 2u/(1-u^2)

### C in S

C(u) = (u**2 + 4*u - 1) / (2*u)

Cleared denominator condition:

```text
W_C^2 = u**4 + 8*u**3 + 18*u**2 - 8*u + 1
denominator_square = 4*u**2
```

### B in S

B(u) = (2*u) / (-(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = (u**2 + 1)**2
denominator_square = (u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (u**2 + 4*u - 1) / (2*u)

Cleared denominator condition:

```text
W_D^2 = u**4 + 8*u**3 + 18*u**2 - 8*u + 1
denominator_square = 4*u**2
```

## delta = 3

A = 2u/(1-u^2)

### C in S

C(u) = (u**2 + 6*u - 1) / (2*u)

Cleared denominator condition:

```text
W_C^2 = u**4 + 12*u**3 + 38*u**2 - 12*u + 1
denominator_square = 4*u**2
```

### B in S

B(u) = (4*u) / (-(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = u**4 + 14*u**2 + 1
denominator_square = (u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (u**2 + 6*u - 1) / (4*u)

Cleared denominator condition:

```text
W_D^2 = u**4 + 12*u**3 + 50*u**2 - 12*u + 1
denominator_square = 16*u**2
```

## delta = 1/2

A = 2u/(1-u^2)

### C in S

C(u) = (u**2 + u - 1) / (2*u)

Cleared denominator condition:

```text
W_C^2 = u**4 + 2*u**3 + 3*u**2 - 2*u + 1
denominator_square = 4*u**2
```

### B in S

B(u) = (-u) / (-(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = u**4 - u**2 + 1
denominator_square = (u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (-u**2 - u + 1) / (u)

Cleared denominator condition:

```text
W_D^2 = u**4 + 2*u**3 - 2*u + 1
denominator_square = u**2
```

## delta = 3/2

A = 2u/(1-u^2)

### C in S

C(u) = (u**2 + 3*u - 1) / (2*u)

Cleared denominator condition:

```text
W_C^2 = u**4 + 6*u**3 + 11*u**2 - 6*u + 1
denominator_square = 4*u**2
```

### B in S

B(u) = (u) / (-(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = u**4 - u**2 + 1
denominator_square = (u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (u**2 + 3*u - 1) / (u)

Cleared denominator condition:

```text
W_D^2 = u**4 + 6*u**3 + 8*u**2 - 6*u + 1
denominator_square = u**2
```

## delta = 4/3

A = 2u/(1-u^2)

### C in S

C(u) = ((u + 3)*(3*u - 1)) / (6*u)

Cleared denominator condition:

```text
W_C^2 = 9*u**4 + 48*u**3 + 82*u**2 - 48*u + 9
denominator_square = 36*u**2
```

### B in S

B(u) = (2*u) / (-3*(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = 9*u**4 - 14*u**2 + 9
denominator_square = 9*(u - 1)**2*(u + 1)**2
```

### D in S

D(u) = ((u + 3)*(3*u - 1)) / (2*u)

Cleared denominator condition:

```text
W_D^2 = 9*u**4 + 48*u**3 + 50*u**2 - 48*u + 9
denominator_square = 4*u**2
```

## delta = 5/4

A = 2u/(1-u^2)

### C in S

C(u) = (2*u**2 + 5*u - 2) / (4*u)

Cleared denominator condition:

```text
W_C^2 = 4*u**4 + 20*u**3 + 33*u**2 - 20*u + 4
denominator_square = 16*u**2
```

### B in S

B(u) = (u) / (-2*(u - 1)*(u + 1))

Cleared denominator condition:

```text
W_B^2 = 4*u**4 - 7*u**2 + 4
denominator_square = 4*(u - 1)**2*(u + 1)**2
```

### D in S

D(u) = (2*u**2 + 5*u - 2) / (u)

Cleared denominator condition:

```text
W_D^2 = 2*(2*u**4 + 10*u**3 + 9*u**2 - 10*u + 2)
denominator_square = u**2
```

